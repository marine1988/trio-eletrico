## P0 — RESOLVIDO (Wave 0)

**Root cause 1:** `contacto.html` carregava apenas `css/style.min.css` (156 bytes, **1 regra CSS**). Nunca carregava `css/style.v1.min.css` (50 KB, 380 regras) — a folha principal. Resultado: 33 classes sem qualquer regra, inputs com estilo default do browser, header fixed de 407px, H-scroll de +32px em **todos** os viewports ≤1440.

**Root cause 2:** `css/fixes.css` (todas as correcções da ronda anterior: hero padding-top 120px, whatsapp offset 32px, gallery-item bg/border, testimonial padding, section 5rem, header alignment) **nunca esteve ligado a nenhuma página**. Todas as correcções estavam mortas.

**Fix:** ligar `style.v1.min.css` + `style.min.css` + camadas de override na ordem correcta em ambas as páginas; criar arquitectura de camadas (`fixes.css` → `components.css` → `overlays.css` → `mobile.css`) para permitir trabalho paralelo sem colisões de ficheiro.

**Evidência medida (audit/audit.mjs antes vs depois):**
| métrica | contacto antes | depois |
|---|---|---|
| H-scroll (320→1440) | +32px em 8/8 viewports | 0 em 8/8 |
| elementos a transbordar | 65-80 | 0 |
| filhos fora do pai | 4 | 0 |
| classes sem regra CSS | 33 | 6 |
| tap targets <44px | 23-26 | 6-21 |

Bónus: imagem 404 da galeria (`photo-1558618666...` → HTTP 404) substituída por `photo-1621905251189...` (verificada 200 + foto correcta de eletricista em quadro).
